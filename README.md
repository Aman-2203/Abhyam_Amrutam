**Steps to Execute: **
1. install req pylib pip cmd : pip install flask werkzeug pymupdf requests google-generativeai python-docx
2. create a .env file in the same directory and store the credentials.
3. execute the app.py to run the code.
